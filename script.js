// script.js – logika strony o psach

document.addEventListener("DOMContentLoaded", () => {
  // Tu będzie kod JS do pobierania psów z API i obsługi formularza
  // Na razie tylko szkielet
  console.log("Strona załadowana!");
});
